# OS / editor noise
.DS_Store
.vscode/
.idea/
*.swp

# Screenshots that may contain secrets (e.g. WHOOP client secret)
Screenshot *.png
node_modules

# Next.js
node_modules/
.next/
next-env.d.ts
tsconfig.tsbuildinfo

# Local env — holds WHOOP_CLIENT_SECRET / USDA_FDC_API_KEY. Never commit.
.env
.env*.local

# Local Claude tooling (keep only the project verify skill in git)
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/verify/
